<Window x:Class="ModernBusinessWpfSample.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:conv="clr-namespace:ModernBusinessWpfSample.Desktop.Converters"
        Title="Modern Business WPF Sample"
        Width="1280" Height="820"
        MinWidth="1100" MinHeight="720"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Background="{DynamicResource AppBg}">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            ResizeBorderThickness="8"
                            GlassFrameThickness="0"
                            CornerRadius="0"
                            UseAeroCaptionButtons="False" />
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <conv:StringEqualsToVisibilityConverter x:Key="StringEqualsToVisibilityConverter" />

        <SolidColorBrush x:Key="AppBg" Color="#F3F6FB" />
        <SolidColorBrush x:Key="Surface" Color="#FFFFFF" />
        <SolidColorBrush x:Key="SurfaceAlt" Color="#F8FAFC" />
        <SolidColorBrush x:Key="BorderSoft" Color="#E2E8F0" />
        <SolidColorBrush x:Key="TextStrong" Color="#162033" />
        <SolidColorBrush x:Key="TextMuted" Color="#64748B" />
        <SolidColorBrush x:Key="Accent" Color="#2563EB" />
        <SolidColorBrush x:Key="AccentHover" Color="#1D4ED8" />
        <SolidColorBrush x:Key="AccentSoft" Color="#EAF1FF" />
        <SolidColorBrush x:Key="SuccessSoft" Color="#ECFDF3" />
        <SolidColorBrush x:Key="Success" Color="#039855" />
        <SolidColorBrush x:Key="TitleBarBg" Color="#0F172A" />
        <SolidColorBrush x:Key="TitleBarBg2" Color="#1E3A8A" />
        <SolidColorBrush x:Key="TitleBarBorder" Color="#1D4ED8" />
        <SolidColorBrush x:Key="TitleBarText" Color="#FFFFFF" />
        <SolidColorBrush x:Key="TitleBarSubtle" Color="#BFDBFE" />
        <SolidColorBrush x:Key="NavigationHover" Color="#F1F5F9" />
        <SolidColorBrush x:Key="RowBorder" Color="#EEF2F7" />
        <SolidColorBrush x:Key="HeaderIconBg" Color="#DBEAFE" />
        <SolidColorBrush x:Key="HeaderBadgeBg" Color="#153E75" />
        <SolidColorBrush x:Key="HeaderUserBg" Color="#172554" />
        <SolidColorBrush x:Key="HeaderBadgeBorder" Color="#3B82F6" />
        <SolidColorBrush x:Key="HeaderOnline" Color="#22C55E" />
        <SolidColorBrush x:Key="HeaderOnlineText" Color="#DCFCE7" />

        <Style x:Key="PageTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource TextStrong}" />
        </Style>

        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="{StaticResource TextMuted}" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>

        <Style x:Key="PanelCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="24" />
            <Setter Property="Background" Value="{StaticResource Surface}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="26" />
            <Setter Property="Margin" Value="0,0,0,18" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="24" ShadowDepth="4" Opacity="0.08" Color="#64748B" />
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MetricCard" TargetType="Border">
            <Setter Property="CornerRadius" Value="20" />
            <Setter Property="Background" Value="{StaticResource Surface}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="22" />
            <Setter Property="Margin" Value="0,0,14,0" />
        </Style>

        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Height" Value="42" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="{StaticResource TextStrong}" />
            <Setter Property="Background" Value="{StaticResource Surface}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12">
                            <ScrollViewer x:Name="PART_ContentHost" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Root" Property="BorderBrush" Value="{StaticResource Accent}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="42" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="10,0" />
            <Setter Property="Foreground" Value="{StaticResource TextStrong}" />
            <Setter Property="Background" Value="{StaticResource Surface}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="42" />
            <Setter Property="Padding" Value="18,0" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="{StaticResource Accent}" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource AccentHover}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.86" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="TitleBarButton" TargetType="Button">
            <Setter Property="Width" Value="36" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#2563EB" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.78" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TitleBarCloseButton" TargetType="Button" BasedOn="{StaticResource TitleBarButton}">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="ToolTip" Value="閉じる" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-2,0,0" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#DC2626" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#991B1B" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GhostButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Foreground" Value="{StaticResource TextStrong}" />
            <Setter Property="Background" Value="{StaticResource SurfaceAlt}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource AccentSoft}" />
                                <Setter Property="Foreground" Value="{StaticResource Accent}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBox">
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
        </Style>
        <Style TargetType="ListBoxItem">
            <Setter Property="Height" Value="48" />
            <Setter Property="Padding" Value="16,0" />
            <Setter Property="Margin" Value="0,4,0,4" />
            <Setter Property="Foreground" Value="{StaticResource TextMuted}" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Root" Background="Transparent" CornerRadius="14" Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="4" />
                                    <ColumnDefinition Width="12" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border x:Name="Indicator" Grid.Column="0" Width="4" CornerRadius="2" Background="Transparent" />
                                <ContentPresenter Grid.Column="2" VerticalAlignment="Center" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource NavigationHover}" />
                                <Setter Property="Foreground" Value="{StaticResource TextStrong}" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource AccentSoft}" />
                                <Setter TargetName="Indicator" Property="Background" Value="{StaticResource Accent}" />
                                <Setter Property="Foreground" Value="{StaticResource Accent}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="GridLinesVisibility" Value="None" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="RowHeight" Value="46" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Foreground" Value="{StaticResource TextStrong}" />
            <Setter Property="HorizontalGridLinesBrush" Value="Transparent" />
            <Setter Property="VerticalGridLinesBrush" Value="Transparent" />
            <Setter Property="CanUserResizeRows" Value="False" />
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{StaticResource SurfaceAlt}" />
            <Setter Property="Foreground" Value="{StaticResource TextMuted}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="Height" Value="44" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderSoft}" />
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="{StaticResource Surface}" />
            <Setter Property="BorderBrush" Value="{StaticResource RowBorder}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource SurfaceAlt}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource AppBg}">
        <Grid.RowDefinitions>
            <RowDefinition Height="72" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Pure WPF custom chrome. The header acts as the drag surface and hosts window controls. -->
        <Border Grid.Row="0" Grid.ColumnSpan="2" BorderBrush="{StaticResource TitleBarBorder}" BorderThickness="0,0,0,1" Padding="22,0" MouseLeftButtonDown="AppHeader_MouseLeftButtonDown">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop x:Name="TitleGradientStart" Color="#0F172A" Offset="0" />
                    <GradientStop x:Name="TitleGradientEnd" Color="#1E3A8A" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Width="38" Height="38" CornerRadius="13" Background="{StaticResource HeaderIconBg}" Margin="0,0,14,0">
                        <TextBlock Text="業" FontSize="16" FontWeight="Bold" Foreground="{StaticResource Accent}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <StackPanel>
                        <TextBlock Text="Modern Business WPF Sample" FontSize="19" FontWeight="SemiBold" Foreground="{StaticResource TitleBarText}" />
                        <TextBlock Text="Pure WPF / MVVM / DI / Clean-ish Layers" FontSize="12" Foreground="{StaticResource TitleBarSubtle}" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border CornerRadius="15" Background="{StaticResource HeaderBadgeBg}" BorderBrush="{StaticResource HeaderBadgeBorder}" BorderThickness="1" Padding="13,8" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Fill="{StaticResource HeaderOnline}" Margin="0,0,8,0" />
                            <TextBlock Text="Online" FontWeight="SemiBold" Foreground="{StaticResource HeaderOnlineText}" FontSize="12" />
                        </StackPanel>
                    </Border>
                    <Border CornerRadius="15" Background="{StaticResource HeaderUserBg}" BorderBrush="{StaticResource HeaderBadgeBorder}" BorderThickness="1" Padding="13,8" Margin="0,0,10,0">
                        <TextBlock Text="Admin User" FontWeight="SemiBold" Foreground="White" FontSize="12" />
                    </Border>
                    <Button Style="{StaticResource TitleBarButton}" Content="−" ToolTip="最小化" Click="MinimizeButton_Click" />
                    <Button x:Name="MaximizeRestoreButton" Style="{StaticResource TitleBarButton}" Content="□" ToolTip="最大化" Click="MaximizeRestoreButton_Click" />
                    <Button Style="{StaticResource TitleBarCloseButton}" Content="×" Click="CloseButton_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Grid.Column="0" Background="{StaticResource Surface}" Padding="24" BorderBrush="{StaticResource BorderSoft}" BorderThickness="0,0,1,0">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Top" Margin="0,18,0,28">
                    <Border Width="48" Height="48" CornerRadius="16" Background="{StaticResource AccentSoft}" HorizontalAlignment="Left">
                        <TextBlock Text="業" FontSize="20" FontWeight="Bold" Foreground="{StaticResource Accent}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <TextBlock Text="Business Hub" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextStrong}" Margin="0,16,0,0" />
                    <TextBlock Text="Sales operations sample" Foreground="{StaticResource TextMuted}" Margin="0,6,0,0" />
                </StackPanel>

                <Border DockPanel.Dock="Bottom" CornerRadius="18" Background="{StaticResource SurfaceAlt}" Padding="16" Margin="0,24,0,0" BorderBrush="{StaticResource BorderSoft}" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="Architecture" FontWeight="SemiBold" Foreground="{StaticResource TextStrong}" />
                        <TextBlock Text="Pure WPF / MVVM / DI / UseCase" Foreground="{StaticResource TextMuted}" FontSize="12" TextWrapping="Wrap" Margin="0,6,0,0" />
                    </StackPanel>
                </Border>

                <ListBox ItemsSource="{Binding Sections}" SelectedItem="{Binding SelectedSection}" />
            </DockPanel>
        </Border>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Padding="38,38,38,16" Background="{StaticResource AppBg}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Operations Workspace" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextStrong}" />
                        <TextBlock Text="Dashboard / Customers / Orders / Reports を横断する業務オペレーション画面" FontSize="15" Foreground="{StaticResource TextMuted}" Margin="0,8,0,0" />
                    </StackPanel>
                    <Border Grid.Column="1" CornerRadius="18" Background="{StaticResource Surface}" BorderBrush="{StaticResource BorderSoft}" BorderThickness="1" Padding="16,10" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="10" Height="10" Fill="{StaticResource Success}" Margin="0,0,8,0" />
                            <TextBlock Text="Sample Environment" FontWeight="SemiBold" Foreground="{StaticResource TextStrong}" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="38,8,38,28">
                    <Grid Visibility="{Binding SelectedSection, Converter={StaticResource StringEqualsToVisibilityConverter}, ConverterParameter=Dashboard}">
                        <StackPanel>
                            <Border Style="{StaticResource PanelCard}">
                                <StackPanel>
                                    <DockPanel LastChildFill="False" Margin="0,0,0,24">
                                        <StackPanel DockPanel.Dock="Left">
                                            <TextBlock Text="ダッシュボード" Style="{StaticResource PageTitle}" />
                                            <TextBlock Text="業務状況のサマリーと外部APIの疎通状態" Foreground="{StaticResource TextMuted}" Margin="0,6,0,0" />
                                        </StackPanel>
                                        <Button DockPanel.Dock="Right" Style="{StaticResource PrimaryButton}" Content="更新" Width="110" Command="{Binding RefreshDashboardCommand}" />
                                    </DockPanel>
                                    <UniformGrid Columns="4">
                                        <Border Style="{StaticResource MetricCard}">
                                            <StackPanel>
                                                <TextBlock Text="有効顧客" Foreground="{StaticResource TextMuted}" />
                                                <TextBlock Text="{Binding ActiveCustomers}" FontSize="34" FontWeight="Bold" Foreground="{StaticResource TextStrong}" Margin="0,8,0,0" />
                                                <TextBlock Text="active accounts" Foreground="{StaticResource TextMuted}" FontSize="12" />
                                            </StackPanel>
                                        </Border>
                                        <Border Style="{StaticResource MetricCard}">
                                            <StackPanel>
                                                <TextBlock Text="今月受注" Foreground="{StaticResource TextMuted}" />
                                                <TextBlock Text="{Binding MonthlyOrders}" FontSize="34" FontWeight="Bold" Foreground="{StaticResource TextStrong}" Margin="0,8,0,0" />
                                                <TextBlock Text="orders this month" Foreground="{StaticResource TextMuted}" FontSize="12" />
                                            </StackPanel>
                                        </Border>
                                        <Border Style="{StaticResource MetricCard}">
                                            <StackPanel>
                                                <TextBlock Text="今月売上" Foreground="{StaticResource TextMuted}" />
                                                <TextBlock Text="{Binding MonthlySales, StringFormat=¥{0:N0}}" FontSize="34" FontWeight="Bold" Foreground="{StaticResource TextStrong}" Margin="0,8,0,0" />
                                                <TextBlock Text="sales amount" Foreground="{StaticResource TextMuted}" FontSize="12" />
                                            </StackPanel>
                                        </Border>
                                        <Border Style="{StaticResource MetricCard}" Margin="0">
                                            <StackPanel>
                                                <TextBlock Text="API状態" Foreground="{StaticResource TextMuted}" />
                                                <TextBlock Text="{Binding ApiHealth}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource Success}" TextWrapping="Wrap" Margin="0,12,0,0" />
                                                <TextBlock Text="typed HttpClient" Foreground="{StaticResource TextMuted}" FontSize="12" />
                                            </StackPanel>
                                        </Border>
                                    </UniformGrid>
                                </StackPanel>
                            </Border>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="16" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Style="{StaticResource PanelCard}" Padding="24">
                                    <StackPanel>
                                        <TextBlock Text="実装ポイント" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextStrong}" />
                                        <TextBlock Text="ViewModel → UseCase → Repository/API/Exporter の流れで、WPF依存をPresentation層に閉じ込めています。" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" Margin="0,8,0,0" />
                                    </StackPanel>
                                </Border>
                                <Border Grid.Column="2" Style="{StaticResource PanelCard}" Padding="24">
                                    <StackPanel>
                                        <TextBlock Text="保存先" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextStrong}" />
                                        <TextBlock Text="%LOCALAPPDATA%\ModernBusinessWpfSample" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" Margin="0,8,0,0" />
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </StackPanel>
                    </Grid>

                    <Grid Visibility="{Binding SelectedSection, Converter={StaticResource StringEqualsToVisibilityConverter}, ConverterParameter=Customers}">
                        <Border Style="{StaticResource PanelCard}">
                            <StackPanel>
                                <DockPanel LastChildFill="False" Margin="0,0,0,22">
                                    <StackPanel DockPanel.Dock="Left">
                                        <TextBlock Text="顧客管理" Style="{StaticResource PageTitle}" />
                                        <TextBlock Text="顧客マスタをローカルJSONリポジトリへ保存します" Foreground="{StaticResource TextMuted}" Margin="0,6,0,0" />
                                    </StackPanel>
                                    <Button DockPanel.Dock="Right" Style="{StaticResource GhostButton}" Content="再読込" Width="110" Command="{Binding LoadCustomersCommand}" />
                                </DockPanel>

                                <Border CornerRadius="18" Background="{StaticResource SurfaceAlt}" BorderBrush="{StaticResource BorderSoft}" BorderThickness="1" Padding="18" Margin="0,0,0,20">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1.2*" />
                                            <ColumnDefinition Width="1.5*" />
                                            <ColumnDefinition Width="1*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                            <TextBlock Text="顧客名" Style="{StaticResource LabelText}" />
                                            <TextBox Style="{StaticResource InputBox}" Text="{Binding NewCustomerName, UpdateSourceTrigger=PropertyChanged}" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Margin="0,0,12,0">
                                            <TextBlock Text="メール" Style="{StaticResource LabelText}" />
                                            <TextBox Style="{StaticResource InputBox}" Text="{Binding NewCustomerEmail, UpdateSourceTrigger=PropertyChanged}" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="2" Margin="0,0,12,0">
                                            <TextBlock Text="区分" Style="{StaticResource LabelText}" />
                                            <TextBox Style="{StaticResource InputBox}" Text="{Binding NewCustomerSegment, UpdateSourceTrigger=PropertyChanged}" />
                                        </StackPanel>
                                        <Button Grid.Column="3" Style="{StaticResource PrimaryButton}" Content="顧客追加" Width="120" VerticalAlignment="Bottom" Command="{Binding AddCustomerCommand}" />
                                    </Grid>
                                </Border>

                                <Border CornerRadius="18" BorderBrush="{StaticResource BorderSoft}" BorderThickness="1" Background="{StaticResource Surface}" Padding="1">
                                    <DataGrid ItemsSource="{Binding Customers}" AutoGenerateColumns="False" CanUserAddRows="False" Height="430">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="コード" Binding="{Binding Code}" Width="110" />
                                            <DataGridTextColumn Header="顧客名" Binding="{Binding Name}" Width="*" />
                                            <DataGridTextColumn Header="メール" Binding="{Binding Email}" Width="260" />
                                            <DataGridTextColumn Header="区分" Binding="{Binding Segment}" Width="140" />
                                            <DataGridCheckBoxColumn Header="有効" Binding="{Binding IsActive}" Width="80" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Border>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Grid Visibility="{Binding SelectedSection, Converter={StaticResource StringEqualsToVisibilityConverter}, ConverterParameter=Orders}">
                        <Border Style="{StaticResource PanelCard}">
                            <StackPanel>
                                <DockPanel LastChildFill="False" Margin="0,0,0,22">
                                    <StackPanel DockPanel.Dock="Left">
                                        <TextBlock Text="受注管理" Style="{StaticResource PageTitle}" />
                                        <TextBlock Text="選択した顧客に対して受注を登録します" Foreground="{StaticResource TextMuted}" Margin="0,6,0,0" />
                                    </StackPanel>
                                    <Button DockPanel.Dock="Right" Style="{StaticResource GhostButton}" Content="再読込" Width="110" Command="{Binding LoadOrdersCommand}" />
                                </DockPanel>

                                <Border CornerRadius="18" Background="{StaticResource SurfaceAlt}" BorderBrush="{StaticResource BorderSoft}" BorderThickness="1" Padding="18" Margin="0,0,0,20">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*" />
                                            <ColumnDefinition Width="1*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                            <TextBlock Text="顧客" Style="{StaticResource LabelText}" />
                                            <ComboBox ItemsSource="{Binding Customers}" SelectedItem="{Binding SelectedCustomer}" DisplayMemberPath="Name" />
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Margin="0,0,12,0">
                                            <TextBlock Text="金額" Style="{StaticResource LabelText}" />
                                            <TextBox Style="{StaticResource InputBox}" Text="{Binding NewOrderAmount, UpdateSourceTrigger=PropertyChanged}" />
                                        </StackPanel>
                                        <Button Grid.Column="2" Style="{StaticResource PrimaryButton}" Content="受注追加" Width="120" VerticalAlignment="Bottom" Command="{Binding AddOrderCommand}" />
                                    </Grid>
                                </Border>

                                <Border CornerRadius="18" BorderBrush="{StaticResource BorderSoft}" BorderThickness="1" Background="{StaticResource Surface}" Padding="1">
                                    <DataGrid ItemsSource="{Binding Orders}" AutoGenerateColumns="False" CanUserAddRows="False" Height="430">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="受注番号" Binding="{Binding OrderNo}" Width="150" />
                                            <DataGridTextColumn Header="顧客名" Binding="{Binding CustomerName}" Width="*" />
                                            <DataGridTextColumn Header="受注日" Binding="{Binding OrderedAt, StringFormat=yyyy/MM/dd HH:mm}" Width="170" />
                                            <DataGridTextColumn Header="金額" Binding="{Binding Amount, StringFormat=¥{0:N0}}" Width="150" />
                                            <DataGridTextColumn Header="状態" Binding="{Binding Status}" Width="130" />
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Border>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Grid Visibility="{Binding SelectedSection, Converter={StaticResource StringEqualsToVisibilityConverter}, ConverterParameter=Reports}">
                        <Border Style="{StaticResource PanelCard}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="260" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="レポート" Style="{StaticResource PageTitle}" />
                                    <TextBlock Text="受注一覧をCSVとしてデスクトップに出力します。帳票基盤やExcel出力へ差し替える入口です。" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" Margin="0,8,0,24" />
                                    <Button Style="{StaticResource PrimaryButton}" Content="受注CSVを出力" Width="180" HorizontalAlignment="Left" Command="{Binding ExportOrdersReportCommand}" />
                                    <TextBlock Text="{Binding ReportMessage}" Foreground="{StaticResource TextStrong}" Margin="0,22,0,0" TextWrapping="Wrap" />
                                </StackPanel>
                                <Border Grid.Column="1" CornerRadius="22" Background="{StaticResource AccentSoft}" Padding="22" Margin="26,0,0,0">
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="CSV" FontSize="42" FontWeight="Bold" Foreground="{StaticResource Accent}" />
                                        <TextBlock Text="Infrastructure層の IReportExporter を差し替えれば、PDF/Excel出力にも拡張できます。" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" Margin="0,8,0,0" />
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </Grid>

                    <Grid Visibility="{Binding SelectedSection, Converter={StaticResource StringEqualsToVisibilityConverter}, ConverterParameter=Settings}">
                        <Border Style="{StaticResource PanelCard}">
                            <StackPanel>
                                <TextBlock Text="設定 / アーキテクチャメモ" Style="{StaticResource PageTitle}" />
                                <Border CornerRadius="18" Background="{StaticResource SurfaceAlt}" BorderBrush="{StaticResource BorderSoft}" BorderThickness="1" Padding="20" Margin="0,22,0,0">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="表示テーマ" FontSize="17" FontWeight="SemiBold" Foreground="{StaticResource TextStrong}" />
                                            <TextBlock Text="ライトモードとダークモードを切り替えます。" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" Margin="0,8,0,0" />
                                        </StackPanel>
                                        <Button x:Name="ThemeToggleButton" Grid.Column="1" Style="{StaticResource PrimaryButton}" Content="ダークモードに切替" Width="170" Margin="24,0,0,0" VerticalAlignment="Center" Click="ThemeToggleButton_Click" />
                                    </Grid>
                                </Border>
                                <Grid Margin="0,18,0,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="16" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <Border CornerRadius="18" Background="{StaticResource SurfaceAlt}" BorderBrush="{StaticResource BorderSoft}" BorderThickness="1" Padding="20">
                                        <StackPanel>
                                            <TextBlock Text="Layering" FontSize="17" FontWeight="SemiBold" Foreground="{StaticResource TextStrong}" />
                                            <TextBlock Text="Desktop → Application → Domain。InfrastructureはApplicationのinterfaceを実装します。" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" Margin="0,8,0,0" />
                                        </StackPanel>
                                    </Border>
                                    <Border Grid.Column="2" CornerRadius="18" Background="{StaticResource SurfaceAlt}" BorderBrush="{StaticResource BorderSoft}" BorderThickness="1" Padding="20">
                                        <StackPanel>
                                            <TextBlock Text="Data Path" FontSize="17" FontWeight="SemiBold" Foreground="{StaticResource TextStrong}" />
                                            <TextBlock Text="%LOCALAPPDATA%\ModernBusinessWpfSample" Foreground="{StaticResource TextMuted}" TextWrapping="Wrap" Margin="0,8,0,0" />
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </Grid>
                </StackPanel>
            </ScrollViewer>

            <Border Grid.Row="2" Background="{StaticResource AppBg}" Padding="38,0,38,26">
                <Border CornerRadius="16" Background="{StaticResource Surface}" BorderBrush="{StaticResource BorderSoft}" BorderThickness="1" Padding="16,11">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="8" Height="8" Fill="{StaticResource Success}" Margin="0,0,10,0" />
                        <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource TextMuted}" />
                    </StackPanel>
                </Border>
            </Border>
        </Grid>
    </Grid>
</Window>
